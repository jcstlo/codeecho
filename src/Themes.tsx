import { createTheme } from '@mui/material';

export const monospaceFontTheme = createTheme({
  typography: {
    fontFamily: "Martian Mono",
    fontSize: 12,
  },
});

export const headingFontTheme = createTheme({
  typography: {
    fontFamily: "Open Sans",
  },
});